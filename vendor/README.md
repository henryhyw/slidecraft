# Vendored development sources

This directory preserves upstream source assets used to prepare bundled resources. Runtime copies are packaged under `src/slidecraft/starter_resources/` with their licenses and metadata.
