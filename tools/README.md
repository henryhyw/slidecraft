# Development tools

This directory contains maintenance and historical regression utilities. It is separate from the installed `slidecraft` runtime.

`historical_regression/` preserves the scripts used to develop and validate the original architecture-slide reconstruction. Production workflows use the package capabilities and command-line interface under `src/slidecraft/`.
