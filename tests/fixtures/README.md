# Test fixtures

`unit/` contains small provider responses for focused tests.

`architecture/` contains a historical end-to-end regression sample. It preserves earlier schema vocabulary so migration and reconstruction stay testable. These files are test data and are never installed as user projects.
