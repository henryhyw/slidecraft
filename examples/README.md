# Examples

This directory is reserved for small, redistributable examples that contain no user project data or generated runtime state. Live projects belong in a user-selected folder or the ignored local `workspace/` directory.
