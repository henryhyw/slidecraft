# Local workspaces

Slidecraft projects, generated evidence, deliverables, temporary files, and model caches live here during local development. The directory is excluded from source control so presentation content and runtime data do not enter the distributable package.

The dashboard and Agent capabilities use the project registry under the user data directory. A project can also live anywhere else on the computer.
